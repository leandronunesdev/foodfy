<div class="user-form">

    <div class="">
        <h3>Nome</h3>
        <input type="text" name="name" value="{{user.name}}" placeholder="Digite o nome completo">
    </div>

    <div class="">
        <h3>Email</h3>
        <input type="email" name="email" value="{{user.email}}" placeholder="Digite o email">
    </div>    

    <div class="checkbox">    
        <input type="checkbox" name="is_admin" id="is_admin" {% if user.is_admin == true %} checked {% endif%}>
        <label for="is_admin">Administrador</label>
    </div>

</div>


